
<?php
	error_reporting(E_ALL);
	ini_set("display_errors", 1);

$servername = "localhost";
$username = "root";
$password = "fraktio";
$dbname = "osake";

// Create connection
$conn = new mysqli($servername, $username, $password, $dbname);
// Check connection
if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}

$sql = "SELECT company, final FROM rate WHERE date = '2014-1-1'";
$result = $conn->query($sql);
$looper = array();
if ($result->num_rows > 0) {
    // output data of each row
    while($row = $result->fetch_assoc()) {
//	        echo "yritys " . $row["company"]. "<br>";
//	$looper[] .= "" . $row["company"]. "";
	$looper[] .= array("yrityksenimi" => "jtn");
    }
} else {
    echo "0 results";
}
$conn->close();

?>


<pre><?php print_r($looper); ?></pre>
